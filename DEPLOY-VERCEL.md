# Vercel deployment

The current system is Nuxt + Express + MongoDB Atlas.

Use [the current deployment guide](DEPLOY-BACKEND-VERCEL.md) for the two existing projects:

- `booking-room-continue` (frontend)
- `booking-room-continue-backend` (API)

The previous tunnel/MariaDB instructions have been superseded. For a local stack, use [README.md](README.md).
