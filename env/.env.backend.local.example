# Backend local (Docker Compose) for Vercel + Tunnel

NODE_ENV=production

# Database created by docker-compose (MariaDB service)
DB_HOST=db
DB_PORT=3306
DB_USER=dbmaster
DB_PASSWORD=change_me
DB_NAME=bookingruk_db

# Auth
JWT_SECRET=change_this_to_a_long_random_string

# During local testing before tunnel
FRONTEND_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000
COOKIE_DOMAIN=

# Google OAuth (use real values from Google Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback

